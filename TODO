- Move common utilities into a util.js file.
- Namespace everything to avoid conflicts with site js, suggestion of tonyscelfo-cik as namespace.
- Add a url bar notification that a page has been processed.
- Allow users to click the notification to temporarily disable processing.
